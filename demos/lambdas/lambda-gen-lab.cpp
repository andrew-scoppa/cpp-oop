#include <iostream>

// add the requisite header(s)

using std::cout, std::endl;

// return a lambda that will square the incoming argument (int)->int
auto sqrGen() {

}

// return a lambda that will cube the incoming argument (int)->int
auto cubeGen() {

}

int ar[] = {10, 20, 30, 40, 50};

int main(int argc, char* argv[]) {
    // declare an std::vector v to match the signature of the gen functions

    // push sqrGen and cubeGen into the vector

    // for every number n in vector:  print n, square of n and cube of n
    // sample row: 
    // n: 10   Squared: 100    Cubed: 1000
 
 	return 0;
}
